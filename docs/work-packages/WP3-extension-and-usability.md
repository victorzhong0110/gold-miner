# WP3：Chrome 试验扩展与真实安装验证

登记日期：2026-09-21。上级入口：[工作包总览](README.md)。本文件是任务定义，不是执行授权或完成报告。

- 对应原12周计划：第 5–6 周；粗估：7–10 工程人日。允许复用已审查代码，不重复计工作量。
- 依赖：WP1 工程基线；WP2 为产品范围决策依据。现有 PR #2 可修复和验证，不需要重写；大规模扩展功能仍需价值证据。
- 2026-09-21 状态：PR #2 有 MV3 源码与安装包候选；上一轮密钥隔离修复有离线证据。Chrome 真机安装、端到端 UI 和个人模型连接未验收。
- 写入负责人、平台/会话、工作区、执行分支：未分配（unknown）；当前没有自动启动的任务。

## 任务与验收

| ID | 任务 | 执行内容 | 验收标准 |
|---|---|---|---|
| WP3-01 | 审查整合现有扩展 | 复核 PR #2 与 main 差异、分发包同步、实际可用与未实现项。 | 原作者、审查者、合并者责任明确；保留 Draft 直到阻塞问题结清。 |
| WP3-02 | 完成首次使用流程 | 阅读语言、兴趣、BYOK 配置、连接状态、无需新模型调用的降级、修改和清空入口。 | 未配置时说明覆盖；配置后真正尝试模型，不能只改 hasModel 标签。 |
| WP3-03 | 原页面搜索与预览 | 保留原查询，展示少量候选、用途、来源及可追溯相关原因，支持关闭和返回。 | 中英文 UI 可读；代码实现细节不替代用户解释；来源不明确不编造理由。 |
| WP3-04 | 仓库页探索 | 基于真实元数据/topics/描述构造候选来源，接入反馈按钮。 | 普通仓库名也有实际候选获取路径；不得仅搜索 owner/repo 再过滤自己。 |
| WP3-05 | 安全边界与导入链接 | 隔离密钥存储，核对消息发送者权限、端点权限申请、远端文本、候选链接。 | 含未结 R3：不信任导入 html_url；校验仓库名并生成 GitHub URL；密钥不进 content script、日志或导出。 |
| WP3-06 | 导航与任务生命周期 | 初次打开、站内导航、前进后退、关闭、取消、重复事件、后台重启及错误呈现。 | 含未结 R2：取消保持终态、忽略迟到回调，不缓存未完成结果；旧页面不再触发新调用。 |
| WP3-07 | 实机验收 | 在 Chrome 检查安装、搜索、仓库页、设置、隔离、导航、取消与卸载；记录版本和步骤。 | 可用自备浏览器环境由执行者完成，不一律归为 only-owner；缺环境写明；真人自然度另由实际使用者评价。 |
| WP3-08 | 首次外部安装 | 开发者之外至少一人按十分钟内上手流程完成核心操作。 | 保留安装卡点、时间与失败；无人参与不能用自动化脚本替代。 |

## 交付与范围边界

可加载版本包、对应源码 SHA、双语安装说明、浏览器验证报告、问题与修复记录。

修复已知阻塞缺陷、安装流程可实际复现后，才进入小范围体验验收；工程可安装不代表产品效果有效。

实施者在实际任务授权中声明拟改路径，优先复用对应 experiments/、extension/、scripts/ 与 docs/ 下已有材料；本计划不要求为路径占位创建空文件。尚未合并的路径必须注明所在 PR/head，不能当 main 文件引用。

## 验证与交接

执行前从[状态表](STATUS.md)登记唯一写入者、实际工作区与基线 SHA。每个子任务分别回报实现、验证、提交、合并、实际验收，附命令、结果、对应 SHA、证据路径和未运行理由。只运行能解决当前风险的测试；个人凭据与真人反馈缺失时保持待验证。

实际验收通过后由评审方确认本包出口；合并由用户授权。暂停或换执行者时保留工作区，不 reset/clean/stash，不在其他窗口重复派发同一任务。
