# 已知限制（0.1.0-trial）

- H1/E1 增益：**未运行**真实评估，不得对外宣传。
- E2 无目标发现：**无会话**。
- E3 现成翻译对照：**未运行**。
- GitHub 无凭据搜索配额很低；限流会停。
- 无模型时只有很小的固定词表扩展，不是协议 C 组。
- GitHub DOM 选择器会随网站改版失效；找不到锚点时只少一块面板。
- `chrome.storage.local.setAccessLevel(TRUSTED_CONTEXTS)`：离线 mock 已覆盖「调用正确 API、失败则拒存密钥」。真机内容脚本读不到密钥 **未运行**。
- SPA 钩子覆盖 pjax/turbo/pushState；未覆盖的导航可能漏一次刷新。
- 导出包不能保证第三方以后加入的字段都被洗掉；导入只收白名单字段。
- 许可证未定，接收方没有明确再用授权。
- 英文使用者体验未验证。
