# WP1：基线、协议和真实实验准备

登记日期：2026-09-21。上级入口：[工作包总览](README.md)。本文件是任务定义，不是执行授权或完成报告。

- 对应原12周计划：第 1–2 周；粗估：6–10 工程人日。允许复用已审查代码，不重复计工作量。
- 依赖：无前置工作包；开始写入前必须确认唯一负责人和实际工作区。PR #2 的代码仍是未合并候选。
- 2026-09-21 状态：main 已有旧实验脚本；PR #2 有协议、CI、生成客户端和材料候选。不能将候选交付计为 main 已具备或真实验证通过。
- 写入负责人、平台/会话、工作区、执行分支：未分配（unknown）；当前没有自动启动的任务。

## 任务与验收

| ID | 任务 | 执行内容 | 验收标准 |
|---|---|---|---|
| WP1-01 | 核对基线与交接 | 记录 main、PR base/head、工作区、未提交及未推送变更、写入负责人；审查前刷新远端状态。 | 可定位唯一写入者，证据 SHA 明确；不清理或覆盖未知改动。 |
| WP1-02 | 对齐协议与规范 | 核对 AGENTS、v0.4、PR #2 的 v0.5、schema、提示词；清理 stars 下限、字段节号、冻结、扩展开工和验收语义冲突。 | 不得由执行者修改门槛来宣布完成；规范调整单列，由规划/评审方审查。 |
| WP1-03 | 真实模型客户端和配置 | 验证 B/C/M 生成、可注入 HTTP 客户端、超时/认证/解析错误；配置就绪时才执行一次个人模型连接。 | 无 key 可完成实现与模拟验证；只有真实调用记 live，并记录模型、时间、用量/未知，不存 key。 |
| WP1-04 | 接通完整检索执行链 | 将生成输出转换为检索器所需变体，原查询保留、同查询去重、请求预算、字段及语言约束在程序中校验。 | B 原文+1；C/M 原文+最多3；C 的两条目标语言扩展与 M 同语要求可核验；不单靠提示词。 |
| WP1-05 | 冻结评估材料 | 区分 dev、开放评估和已知目标诊断；核验种子来源、日期与文档语言；不设 stars 排除下限。 | 评估前固定题目、模型、提示词、排序、字段与判断标准；在后续提交记录已存在 SHA，不追求文件引用自身 SHA。 |
| WP1-06 | 准备 E2/E3 正式材料 | E2 起点、兴趣、人工候选及准备时间；E3 真实 README、注释/代码、限制和多轮 issue，上下文及出处固定。 | 双向材料分别标记；现有8条冒烟片段与正式材料分开，引用合法且可追溯。 |
| WP1-07 | 离线基线与操作说明 | 整理最小 CI、运行命令、输出 schema 和从新环境开始的说明。 | 失败不能记空成功；fixture/live 明确区分，普通测试不请求付费 API。 |

## 交付与范围边界

基线交接记录、协议冲突决议、可执行客户端与检索串接、冻结清单、E2/E3 材料及离线操作说明。

工程部分经审查可移交 WP2；缺少个人配置只阻塞相应 live 子任务，不阻塞其他独立准备。冻结与真实运行不得用模拟替代。

实施者在实际任务授权中声明拟改路径，优先复用对应 experiments/、extension/、scripts/ 与 docs/ 下已有材料；本计划不要求为路径占位创建空文件。尚未合并的路径必须注明所在 PR/head，不能当 main 文件引用。

## 验证与交接

执行前从[状态表](STATUS.md)登记唯一写入者、实际工作区与基线 SHA。每个子任务分别回报实现、验证、提交、合并、实际验收，附命令、结果、对应 SHA、证据路径和未运行理由。只运行能解决当前风险的测试；个人凭据与真人反馈缺失时保持待验证。

实际验收通过后由评审方确认本包出口；合并由用户授权。暂停或换执行者时保留工作区，不 reset/clean/stash，不在其他窗口重复派发同一任务。
