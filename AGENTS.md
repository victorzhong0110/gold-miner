# AGENTS.md

1. 本仓库处于产品规划 / 1a 实验阶段。
2. 没有可安装扩展。
3. H1 未验证前，禁止写 Chrome 扩展。
4. 禁止写 manifest、content script、service worker。
5. 项目名称待决策，见 docs/decisions/0001。
6. 许可证待决策，见 docs/decisions/0003。
7. 模型可以起草文案和检查脚本。
8. 不得改仓库名。
9. 不得添加 LICENSE。
10. 不得把「建议」写成已决定。
11. 禁止伪造实验结果。
12. 不得编造 GitHub Search 结果。
13. 不得编造 H0 占比、E1 指标、API 额度。
14. 没有真实运行，就写「未运行」。
15. 实验记录必须机器可读。
16. 候选与判定用 JSONL。
17. 字段以 experiments/E1-cross-language-search/protocol.md 第 7 节为准。
18. 改文件前先列出将改路径。
19. 只改任务指定的文件。
20. docs/plan/v0.3.md 只读不改。
21. 后续计划另起 docs/plan/v0.4.md。
22. 不把 API key 写入源码、README、JSONL、译文包。
23. 脚本用环境变量 GITHUB_TOKEN、OPENAI_BASE_URL、OPENAI_MODEL、OPENAI_API_KEY。
24. 每个任务必须有可运行的测试，或明确的「未运行」说明。
25. 禁止留下 TODO 占位实现。
