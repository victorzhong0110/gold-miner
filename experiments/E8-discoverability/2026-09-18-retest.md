# E8 复测记录 2026-09-18（未认证，403 即停，无新结果）

对应：v0.3 第 11 节；v0.4 W7/E8 行；`experiments/E8-discoverability/template.md`；
`docs/reports/w8-first-round-decision-2026-09-17.md` §3-5；[决策 0001](../../docs/decisions/0001-project-name.md)。
性质：配额恢复后复测尝试（1 请求即遇限流，按工程检查停止）。基线仍为 `2026-09-17-probe.md`，本文件不覆盖它。

## 条件

- 日期：2026-09-18（UTC），批次：retest-01，操作者：OpenCode GUI agent（owner-independent，无凭据）。
- 认证：未认证（`GITHUB_TOKEN` 未设置；请求头无 `Authorization`）。密钥未进仓库。
- 当时的仓库名/简介/topics/README/许可证状态：未知（本次唯一请求即 403，未能读取；不得沿用 9-17 基线当作当前状态）。
- 入口：仅 GitHub REST API `GET /repos/{owner}/{repo}`（元数据读取）。搜索 API 查询未发起（见“停止”节）。
- 检查窗口：不适用（未进入排名检查）。

## 尝试（实际运行，失败，未伪造结果）

- `2026-09-18T01:48:41Z`：`GET /repos/victorzhong0110/gold-miner` → `HTTP 403`
  - body `message`: `API rate limit exceeded for 104.30.180.112. (But here's the good news: Authenticated requests get a higher rate limit. Check out the documentation for more details.)`
  - 配额头（原样）：`x-ratelimit-limit: 60`、`x-ratelimit-remaining: 0`、`x-ratelimit-used: 60`、
    `x-ratelimit-resource: core`、`x-ratelimit-reset: 1789698084`（即 `2026-09-18T02:21:24Z`）。
  - 本仓库 `description/topics/license`：未知（403 body 无此字段）。

## 停止（按工程检查，不追加查询）

- 按 `docs/engineering/prerequisites.md` 第 1 节“遇限流即停止”，本次不再发起任何搜索查询。
- 下列查询均未运行（不是 0 结果）：站内名称加用途组、纯用途组、普通搜索引擎组、网页端检查。
  未把接口错误记成零结果。

## 解读

1. 本次未取得任何新元数据与排名位置；E8 结论只能继续引用 9-17 基线并注明条件已可能变化。
2. 失败已记录为配额边界（共享出口核心配额耗尽），不是“未找到”。
3. 下一步仍为：待配额恢复后，用模板新日期文件重跑全部查询并互链 0001。

## 未检查

- GitHub 搜索 API（`GET /search/repositories`）：未运行（本次主动停止）。
- GitHub 网页搜索（登录/未登录、地区）：未检查。
- 普通搜索引擎：未检查。
- Chrome 商店：不适用。

## 复现

```bash
curl -sS -D /tmp/opencode/e8_headers.txt -o /tmp/opencode/e8_repos.json \
  https://api.github.com/repos/victorzhong0110/gold-miner
# 403 即停，不再追加 q "..." 搜索查询。
```

诚实标记：元数据读取 1 次失败（403）；搜索查询未运行；无编造的 `total_count/top3`；
无 H0/E1 指标；零付费调用；无密钥。
