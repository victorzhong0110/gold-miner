# 许可证备选说明（W7 准备，未决定）

状态：待决策。本文只整理备选，不是 [决策 0003](../../docs/decisions/0003-license.md) 的「决定」节，也不把其中的「建议」写成已经选定。

决策 0001、0003 仍是待决策。工作名继续沿用黄金矿工 / Gold Miner；改名和副标题本轮没有决定。仓库名保持 `victorzhong0110/gold-miner`。

## 本轮没有做的事

- 没有在仓库根目录添加 `LICENSE`。
- 没有添加 `NOTICE`。
- 没有修改 README 的许可句子。
- 没有调用 GitHub 接口去改 description、topics 或 license。
- 没有把代码、文档或译文包的许可写成已决定。

开源试用交付前仍需要发起人填写 0003 的决定节。在那之前，仓库可以继续按「保留所有权利」理解：本文件不授予新的使用许可。

## 代码（备选）

两条都还在候选里：

- MIT。0003 把这一条标为建议，理由是写法简单，Chrome 扩展仓库里较常见。若以后放入 Apache-2.0 代码片段，该片段仍要保留原来的许可和版权声明。这只是备选说明，不是本轮决定。
- Apache-2.0。与 EhViewer 相同的许可，移植时少一次许可转换；代价是文件头和 `NOTICE`。同样未选定。

## 文档（备选）

范围是 `docs/`、`experiments/` 和 README。

- CC BY 4.0。0003 把这一条标为建议：可以引用和翻译，需要署名。建议不是决定。
- 与代码使用同一许可。少维护一份文本，但 MIT 写在文档上的含义不清楚。未选定。

## 共享译文数据（原则，整体许可未定）

E5 才会实际产生译文包。0003 只先写了原则，本文件照录，不提前选定包的许可证：

- 译文是原 README 或讨论的衍生作品，受原仓库许可约束。原仓库没有许可证时，不进入公共译文包，只留在本地缓存。
- 公共译文包只收原许可允许衍生和再分发的条目，并逐条记下 SPDX 标识和来源链接。
- 译文包整体的许可留到 E5 首轮之后。0003 写了默认倾向 CC BY-SA 4.0，以便纠错可以回流。倾向不是决定。

## 决定之后才做

这些步骤要等 0003 写上代码许可、文档许可和日期之后再做。本轮不做：

- 根目录增加代码 `LICENSE`。
- 在文档说明里写上文档许可。
- 若选定 Apache-2.0，再补 `NOTICE`。
