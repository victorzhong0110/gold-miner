# 黄金矿工：压缩工作包 WP1–WP6（2026-09-19）

日期：2026-09-19。当前执行入口。
依据：发起人当日压缩计划（忽略日历、尽快做完再汇报）、[v0.4](v0.4.md)、既有 E1/E2/E3 协议。
**不改** [v0.3](v0.3.md)。

## 0 权威覆盖

旧 `AGENTS.md` 曾写「H1 未验证前禁止写 Chrome 扩展」。发起人 2026-09-19 压缩计划 WP3 明确要求可本地加载的 MV3 扩展，并命令在完成本分支 WP1 协议清理（写明新闸门）之后开始扩展工作。

新闸门：

1. WP1 材料、冲突决议、离线 harness 与检查脚本落到本分支。
2. 之后允许编写 manifest / content script / service worker。
3. H1 仍未用真实评估运行证明；扩展是试验载体，不是效果结论。
4. 商店上架、公共推理代理、根目录已决定 LICENSE 仍禁止擅自完成。

v0.4 的 W10「E1 或自动 E2 有收益才做扩展」被本压缩计划在**工程开工**意义上覆盖；**效果判定**仍按 E1/E2/E3 证据，不得用“已经有扩展”代替“已经有增益”。

## 1 工作包对照

| 压缩包 | 对应 v0.4 大致范围 | 本分支交付原则 |
|---|---|---|
| WP1 准备 | W1 + 协议对齐 + CI/操作手册 + BYOK 骨架 | 可离线核对；密钥不入库 |
| WP2 价值 harness | W3/W4/W5/W6 的可运行骨架 | fixture 路径必须绿；live 标未运行 |
| WP3 扩展壳 | W10 最小原页 | 可本地加载；无发起人 key 也能看 UI |
| WP4 发现/个性化/成本 | W9/W11 的轻量实现 | 无模型时降级；可解释来源 |
| WP5 试用包 | W12/W14 脚手架 | 导出不含密钥与私有数据 |
| WP6 Beta 与决策 | W8/W13/W14 报告 | 诚实填已有格；live 格 owner-blocked |

## 2 仍不可伪造

- 不编造 GitHub Search 命中、H0 占比、E1 指标、API 额度、会话原话。
- 没有真人会话 / 没有发起人 API，就写「未运行」或「owner-blocked」。
- 离线 fixture 必须带 `source: fixture`。

## 3 本文件状态

本文件是压缩计划的仓库内落地，不是产品效果报告。效果见 [docs/reports/2026-09-19-wp1-wp6-status.md](../reports/2026-09-19-wp1-wp6-status.md)。
